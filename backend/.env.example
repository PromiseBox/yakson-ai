# Local Cloud SQL Proxy example
# Run Cloud SQL Auth Proxy locally, then point FastAPI at the proxy port.
DATABASE_URL=postgresql+psycopg://DB_USER:DB_PASSWORD@127.0.0.1:55432/yakson
DATABASE_SCHEMA=yakson
DATABASE_AUTO_CREATE=false
DATABASE_CONNECT_TIMEOUT_SECONDS=5

# Comma-separated frontend origins allowed by FastAPI CORS.
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional. When set, FastAPI protects /api/* and expects this value in
# x-yakson-backend-secret from the Next.js server-side proxy.
BACKEND_SHARED_SECRET=

# Optional Neo4j AuraDB graph-first analysis. If these values are present,
# /api/analysis/preview and /api/patients/{id}/analysis/latest try Neo4j first
# and fall back to PostgreSQL rule-preview analysis when graph analysis fails.
NEO4J_ANALYSIS_ENABLED=true
NEO4J_URI=neo4j+s://YOUR_AURA_INSTANCE.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=YOUR_NEO4J_PASSWORD
NEO4J_DATABASE=neo4j

# Production examples:
# DATABASE_URL=postgresql+psycopg://DB_USER:DB_PASSWORD@DB_HOST:5432/yakson
# DATABASE_URL=postgresql+psycopg://DB_USER:DB_PASSWORD@/yakson?host=/cloudsql/promisebox-yakson:asia-northeast3:yakson-postgres
